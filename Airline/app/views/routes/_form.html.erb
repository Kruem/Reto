<%= form_for(@route) do |f| %>
  <% if @route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% @route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="container">
  <div class="row  ">
    <div class="col-md-8 ">
      <div class="field">
        <%= f.label :city_o_id, "Ciudad de Origen" %><br>
        <%=f.select :city_o_id, Citie.all.collect {|u| [u.name,u.country, u.id]},class:"form-control"%>
      </div>
      <br>
      <div class="field">
        <%= f.label :city_d_id, "Ciudad de Destino" %><br>
        <%=f.select :city_d_id, Citie.all.collect {|u| [u.name,u.country, u.id]},class:"form-control"%>
      </div>
      <br>
      <div class="actions">
        <%= f.submit "Guardar", class:"btn btn-danger" %>
      </div>
    <% end %>
    </div>
  </div>
</div>
